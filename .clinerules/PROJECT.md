## Agent skills

### Issue tracker
Issues/PRDs → `.scratch/<NN>-<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels
Status vocab → `Status:` lines in issue files. See `docs/agents/triage-labels.md`.

### Domain docs
One `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.

### Design system
Tokens, components, layouts, conventions → `DESIGN.md`. Read before Blade/Tailwind changes.

### Laravel Boost
Boost MCP tools + Laravel conventions → `AGENTS.md`.